# Base44 entity schema reference

These JSONC files preserve the data models designed in the original Base44 app.
They are documentation/reference only. The self-hosted Global AI Trading Center
does not run the Base44 backend.

Use these schemas when implementing PostgreSQL/SQLAlchemy models and FastAPI
routes on the Trade API.
