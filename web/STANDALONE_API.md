# Standalone Trade API

Frontend API base is configured with `VITE_API_BASE_URL` and defaults to `https://trade-api.hastenload.com`.

No hosted application SDK is required by the standalone client. Authentication, entities, uploads, authorization functions, subscriptions, and user administration must be implemented and enforced by FastAPI before production enablement of those features.
