import React,{useEffect,useState}from"react";import{localStoreApi}from"@/lib/localStoreApi";import PageHeader from"@/components/trading/PageHeader";import{Plug,LockKeyhole,ServerCog}from"lucide-react";export default function BrokerAccounts(){const[accounts,setAccounts]=useState([]),[msg,setMsg]=useState("");useEffect(()=>{localStoreApi.BrokerAccount.list().then(setAccounts).catch(e=>setMsg(e?.message||"Unable to load broker accounts."))},[]);return <div className="space-y-4"><PageHeader title="Broker Accounts" subtitle="Your isolated broker connections and execution environments"/><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole className="w-5 h-5 text-primary"/><div><div className="font-semibold">Credentials stay on the Trading VM</div><div className="text-xs text-muted-foreground mt-1">The browser stores only connection metadata. Broker passwords and trading keys belong on the backend.</div></div></div></div><div className="grid lg:grid-cols-2 gap-3">{accounts.map(a=><div key={a.id} className="rounded-lg border border-border bg-card p-4"><div className="flex items-center gap-2"><Plug className="w-4 h-4 text-primary"/><div className="font-semibold">{a.account_label||a.broker_name||a.broker_type}</div><span className="ml-auto text-xs">{a.status}</span></div></div>)}{!accounts.length&&<div className="rounded-lg border border-dashed border-border p-8 text-center"><ServerCog className="w-6 h-6 mx-auto text-muted-foreground"/><div className="mt-2 font-medium">No broker connected</div></div>}</div>{msg&&<div className="text-xs text-warn">{msg}</div>}</div>}
